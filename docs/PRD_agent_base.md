# PRD — BaseAgent

> **Placeholder — filled in Phase 1.** See `../../CLAUDE_CODE_PLAYBOOK.md` §3 for the full prompt.

The BaseAgent abstraction over `claude -p`: prompt assembly from Skill + injected context, runtime invocation, JSON parsing, cost accounting via the Gatekeeper, protocol-message emission, lifecycle, error handling, and timeout behaviour.
