# PRD — Extension points

> **Placeholder — filled in Phase 1.** See `../../CLAUDE_CODE_PLAYBOOK.md` §3 for the full prompt.

How to extend (closes the HW1 extensibility weakness): add a new agent (subclass BaseAgent + Skill), a new debate topic (config only), a new LLM backend (implement the runtime interface), and a new judge rubric.
