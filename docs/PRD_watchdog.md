# PRD — Watchdog

> **Placeholder — filled in Phase 1.** See `../../CLAUDE_CODE_PLAYBOOK.md` §3 for the full prompt.

Per-call timeouts, keep-alive heartbeat, stall detection, and kill+restart of a stuck or dead process (up to max_restarts_per_agent) with last-context replay so the debate continues.
