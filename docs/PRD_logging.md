# PRD — Structured FIFO logging

> **Placeholder — filled in Phase 1.** See `../../CLAUDE_CODE_PLAYBOOK.md` §3 for the full prompt.

20 files × 500 lines max (config-driven), JSON-lines per event (agent calls, messages, costs, restarts, verdict), so the grader can audit the whole run from logs.
